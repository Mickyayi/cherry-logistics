# 车厘子团购物流系统

一个完整的车厘子团购物流管理系统，连接用户、客服和物流公司。

## 🎯 功能特性

### 用户端（手机网页）
- 提交物流信息（订单号、收货人信息、樱桃品种/大小/数量）
- 查询物流状态（通过姓名+电话）
- **实时物流追踪**（集成快递100 API）

### 客服端（桌面网页）
- 查看所有订单
- 编辑订单信息
- 审核订单

### 物流端（手机网页）
- 查看已审核的新订单
- 一键复制物流信息到剪贴板
- 标记订单为已发货
- 填写快递单号

## 🏗️ 技术架构

- **前端**: React + TypeScript + Tailwind CSS
- **后端**: Cloudflare Workers (JavaScript/Node.js)
- **数据库**: Cloudflare D1 (SQLite)
- **部署**: Cloudflare Pages (前端) + Cloudflare Workers (后端)

## 📦 项目结构

```
cherry/
├── frontend/          # React 前端应用
│   ├── src/
│   │   ├── pages/    # 页面组件
│   │   ├── components/  # 通用组件
│   │   ├── api.ts    # API 请求封装
│   │   └── config.ts # 配置文件
│   └── package.json
├── backend/           # Workers 后端应用
│   ├── src/
│   │   └── index.js  # Worker 入口
│   ├── schema.sql    # 数据库表结构
│   ├── wrangler.toml # Cloudflare 配置
│   └── package.json
└── README.md
```

## 🚀 部署指南

### 1. 创建 Cloudflare D1 数据库

```bash
cd backend
npm install
wrangler login
wrangler d1 create cherry-logistics-db
```

记录返回的 `database_id`，并更新 `backend/wrangler.toml` 中的 `database_id`。

### 2. 初始化数据库表

```bash
wrangler d1 execute cherry-logistics-db --file=schema.sql
```

### 3. 部署后端 Worker

```bash
wrangler deploy
```

部署成功后，记录 Worker 的 URL（例如：`https://cherry-logistics-backend.your-subdomain.workers.dev`）

### 4. 部署前端到 Cloudflare Pages

1. 将代码推送到 GitHub
2. 在 Cloudflare Dashboard 创建 Pages 项目
3. 连接 GitHub 仓库，选择 `frontend` 目录
4. 配置构建设置：
   - 构建命令: `npm run build`
   - 构建输出目录: `dist`
   - 根目录: `frontend`
5. 添加环境变量：
   - `VITE_API_URL`: Worker 的 URL（步骤3中记录的）

### 5. 访问系统
- 用户端: `https://cherry.haofresh.com.cn`
- 客服端: `https://cherry.haofresh.com.cn/admin/login` (密码: 145284)
- 物流端: `https://cherry.haofresh.com.cn/logistics/login` (密码: 145284)

## 🔐 安全配置

默认管理密码为 `145284`，建议在 `backend/src/index.js` 中修改 `ADMIN_PASSCODE` 常量。

## 📱 订单状态流转

```
pending (待审核) 
    ↓ [客服审核通过]
reviewed (已审核) 
    ↓ [物流确认发货]
shipped (已发货) 
    ↓ [填写快递单号]
completed (已完成)
```

## 🎨 界面预览

- 用户端：简洁的移动端设计，两个大按钮入口
- 客服端：桌面端表格视图，适合批量处理订单
- 物流端：移动端标签切换，快速操作

## 📝 订单编号规则

订单ID从1开始自动递增，展示时格式化为三位数字（如 001, 002, ...）

## 🛠️ 本地开发

### 前端
```bash
cd frontend
npm install
npm run dev  # 访问 http://localhost:5173
```

### 后端
```bash
cd backend
npm install
wrangler dev  # 本地测试 Worker
```

## 📄 许可证

本项目仅供学习和个人使用。

